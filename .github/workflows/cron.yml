name: AI Market Briefing Cron Pipeline

on:
  schedule:
    - cron: '5 0 * * *'
    - cron: '0 3 * * *'
    - cron: '5 6 * * *'
    - cron: '5 9 * * *'
  workflow_dispatch: {}

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Internal Pipeline
        run: |
          curl -X POST "${{ secrets.CRON_ENDPOINT }}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json"
