export const buildKeywordTransformPrompt = (input: string) => `
당신은 해외 경제/금융 뉴스를 검색하기 위한 "영문 검색 키워드"를 만드는 전문가입니다.
입력된 검색어가 한국어일 경우, 단순 번역이 아니라 뉴스 검색에 적합한 핵심 키워드 형태의 영어 쿼리로 변환하세요.

요구사항:
1) 출력은 반드시 아래 JSON 형식으로만 출력합니다. (추가 텍스트 금지)
2) query는 영어로 작성합니다.
3) 너무 길지 않게(최대 6~8단어), 검색에 유리한 표현으로 정제합니다.
4) 가능하면 산업/시장/정책 같은 컨텍스트를 보완합니다.
5) 입력이 애매하면 가장 일반적으로 쓰이는 해외 뉴스 검색어로 변환합니다.

출력 형식(JSON):
{
  "query": ""
}

입력 검색어:
${input}
`;


